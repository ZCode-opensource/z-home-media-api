version: '3.9'

services:
  mongo:
    command: mongod --quiet --logpath /var/log/mongodb/mongodb.log
    ports:
      - 27017:27017
  api:
      environment:
        DEVELOPMENT: true
      volumes:
        - ./:/~/zhomemedia